{
  "name": "toastr_1",
  "version": "1.0.0",
  "description": "Toaster – a social automation agent on X (Twitter) for launching and managing tokens on Moonbags.io (Sui Network)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "lint": "eslint 'src/**/*.ts'",
    "test": "jest"
  },
  "dependencies": {
    "@mysten/sui": "^2.8.0",
    "@prisma/client": "^7.5.0",
    "dotenv": "^16.5.0",
    "node-cron": "^4.2.1",
    "twitter-api-v2": "^1.29.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.29",
    "@types/node-cron": "^3.0.11",
    "jest": "^29.7.0",
    "prisma": "^7.5.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.8.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.ts"
    ]
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
